package com.geekbang.supermarket;
/*

//计算超市里利润最高的商品
public class LittleSuperMarket {
    public String superMarketName;
    public String address;
    public int parkingCount;
    public double incomingSum;
    public MerchandiseV2[] merchandises;
    public int[] merchandiseSolrd;

    //返回值是类名，实际返回的值就是类的引用
    public MerchandiseV2 getBiggestProfitMerchandise() {
        MerchandiseV2 curr = null;    //设置利润最高值
        for (int i = 0; i < merchandise.length; i++) {
            MerchandiseV2 m = merchandises[i];

            //如果只有有一个商品
            if (curr = null) {
                curr = m;
                continue;
            } else {
                if (curr.calculateProfit() < m.calculateProfit()) {
                    curr = m;
                }
            }
        }
        return curr;
    }
}

*/